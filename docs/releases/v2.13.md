# GRACEFELL v2.13 release record

## Outcome

GRACEFELL v2.13 lets players pause and resume any active fight without advancing combat or audio.

- Phone and desktop expose one persistent PAUSE/RESUME button.
- The button is a 76×44px semantic DOM control beside SOUND and respects safe-area insets.
- Desktop also supports P and Escape.
- A centered pause card keeps the frozen arena visible and explains how to resume.
- Combat time, player, boss, hazards, RAF, streamed score, and Web Audio all stop together.
- Manual pause remains owned across browser focus and visibility changes.
- Attacks or dodges pressed while paused are discarded before the first resumed frame.

No boss behavior, difficulty value, attack pattern, collision rule, player timing/damage, save
schema, score, music asset, SFX identity, combat-touch geometry, runtime art asset, particle system,
or per-frame rendering workload changed.

## Release identity

| Field | Value |
|---|---|
| Gameplay/package version | v2.13 / `gracefell@2.13.0` |
| Date | 2026-07-24 |
| Production URL | <https://gracefell.alyoechosys.dev> |
| Implementer | Codex, GPT-5 / Codex Desktop |
| Directed by | @jonathanwxh-cell |
| Gameplay revision | Recorded after the reviewed GitHub merge |
| Pull request | Recorded after publication |
| GitHub Actions | Recorded after publication |
| GitHub release | Recorded after production acceptance |

## Design and root cause

The engine already had an interruption-safe pause path for hidden tabs and focused semantic
settings. Players could not invoke it deliberately, and collapsing all pause causes into one
boolean would let one cause incorrectly resume another.

v2.13 adds `manualPaused` as a third owner. `syncPauseState()` pauses while manual, interruption, or
settings-focus ownership remains. Both the DOM control and P/Escape call `togglePause()`. Resume
clears paused combat input before restarting with a fresh timestamp.

The semantic control remains live while RAF is stopped. It returns focus to the canvas after every
activation and publishes immediate engine-to-React state changes, avoiding a stale RESUME label.

## Local acceptance

Accepted production-build gate:

- Command: `npm run qa`
- Result: `ok: true`, `nErrors: 0`
- Result file:
  `C:\Users\Greyf\AppData\Local\Temp\gracefell-qa-pause-local-4\result.json`
- Desktop capture:
  `C:\Users\Greyf\AppData\Local\Temp\gracefell-qa-pause-local-4\desktop-paused.png`
- True-touch capture:
  `C:\Users\Greyf\AppData\Local\Temp\gracefell-qa-pause-local-4\touch-paused.png`

The suite verified desktop 1280×800, mobile 390×844, and a dedicated 390×844
`hasTouch + isMobile` context. Pause-specific assertions cover:

| Contract | Accepted result |
|---|---|
| Touch target | 76×44px, visible, on-screen |
| Desktop activation | P pauses; Escape resumes |
| Phone activation | real touchscreen tap pauses and resumes |
| Simulation | fight clock, player state, boss state frozen |
| Loop | RAF stopped while paused, running after resume |
| Audio | AudioContext suspended while paused |
| Input | light attack entered during pause cleared on resume |
| Semantics | label, `aria-pressed`, live status update immediately |
| Focus | canvas owns input after button pause and resume |

Two deliberately repeated candidate runs were useful:

1. The first found first-gesture audio initialization occurring after the pause transition and
   reawakening the AudioContext. Initialization now precedes keyboard pause.
2. The second found the engine resumed correctly while the React label waited for the 250ms status
   poll. Manual pause now emits an immediate UI notification.

The third complete run passed with zero errors. Earlier isolated cold-audio budget variance was
unrelated to pause behavior and did not recur in the accepted run.

## Production acceptance

This section is completed only after the reviewed merge is fast-forwarded to
`/home/alyosha/apps/gracefell`, built on the host, the `gracefell` user service is restarted, and the
same gate is replayed against the public URL.

## Changed from v2.12.1

- Added explicit manual pause ownership.
- Added a persistent 76×44px semantic PAUSE/RESUME control.
- Added P and Escape shortcuts.
- Added a frozen-arena pause card.
- Added immediate shell synchronization and paused-input disposal.
- Expanded desktop/mobile/true-touch QA with pause/resume contracts and screenshots.
