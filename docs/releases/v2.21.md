# GRACEFELL v2.21 release record

Date: 2026-07-25
Owner: @jonathanwxh-cell
Implementation and verification: Codex (GPT-5 / Codex Desktop)

## Scope

This release resolves the six remaining viable enhancement issues:

- #57 buffered post-roll slash without iframe cancellation;
- #58 rear-arc FLANK reward (1.25x damage, 1.5x poise; no windup/stagger stacking);
- #60 explicit post-score REMAIN and exactly one-step ASCEND actions;
- #62 run-local damage mix, phase splits, recent wounds, and grade guidance;
- #63 local 1080x1350 victory PNG with Web Share/file-download fallback;
- #64 mirrored joystick/action layout persisted in save schema v7.

It also replaces vague Oath summaries with the already-active damage, chain,
flask, poise, and no-stagger rules; separates combat callout lanes; clears stale
damage text at terminal transitions; and fixes an AudioParam automation overlap
found by the expanded gate.

## Safety and performance boundaries

- Roll slash starts only after the complete dodge and costs 10 stamina.
- FLANK is disabled during windup and stagger, so it cannot multiply EXECUTE.
- Chronicle data is run-local and presentation-only.
- Ascension is unavailable until the existing 4.5-second victory hold ends.
- Share rendering is one user-triggered offscreen canvas; no runtime image,
  network request, third-party service, or persistent render pass was added.
- Mirroring changes the shared geometry and acquisition rules, not hitbox size.
- Difficulty values, grading, weather, music masters, and runtime assets are unchanged.

## Local acceptance

Candidate bundle:

- JavaScript: 384.16 kB (118.64 kB gzip)
- CSS: 18.41 kB (4.41 kB gzip)

Gates:

- `npm test`: 19/19
- `npm run lint`: pass
- `npm run build`: pass
- `npm run qa`: `ok=true`, `nErrors=0`
- focused v2.21 mobile lane: `ok=true`, `nErrors=0`

The focused lane records a 390x844 victory chronicle screenshot and validates a
downloaded PNG, mirrored geometry/save round-trip, the complete roll before
roll-slash, exact front/rear damage, and Ascension from Measured 0 to Oath +1.
Artifacts are written under the platform temp folder
`gracefell-qa/v2.21`; they are evidence, not runtime assets.

## GitHub and production receipts

- Reviewed implementation PR:
  [#66](https://github.com/jonathanwxh-cell/gracefell/pull/66)
- Clean-runner acceptance:
  [GitHub Actions run 30139153859](https://github.com/jonathanwxh-cell/gracefell/actions/runs/30139153859),
  successful in 1m48s with deterministic install, lint, 19 unit tests, build,
  the complete playtest, focused v2.21 lane, and artifact upload.
- Merged gameplay SHA:
  [`6bc004db31e9e7a2e255138b7b943b697a645551`](https://github.com/jonathanwxh-cell/gracefell/commit/6bc004db31e9e7a2e255138b7b943b697a645551)
- Host checkout:
  `/home/alyosha/apps/gracefell` clean on `main` at the merged SHA.
- Production bundle:
  `assets/index-BPSr4UzU.js` and `assets/index-Sz31NS6X.css`
- Service:
  `gracefell.service` active from 2026-07-25 09:49:35 +08 with
  `NRestarts=0`.
- Health:
  loopback `127.0.0.1:8491/health` and public
  `https://gracefell.alyoechosys.dev/health` both returned
  `{"ok":true,"app":"gracefell"}`; public HTTP status was 200.
- Public acceptance:
  both `qa/verify.cjs` and `qa/v221.cjs` ran against
  `https://gracefell.alyoechosys.dev/` and returned `ok=true` with zero errors.

The public replay covers all earlier difficulty, boss-pattern, combat, retry,
pause, audio, weather, navigation, scoring, migration, and accessibility
contracts plus the six v2.21 issue-specific checks. The release was therefore
verified before issue closeout rather than inferred from a successful deploy.
