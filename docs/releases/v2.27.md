# GRACEFELL v2.27 release record — earned offense

Date: 2026-07-27
Implementation and verification: Codex (GPT-5 / Codex Desktop)
Direction: [@jonathanwxh-cell](https://github.com/jonathanwxh-cell)
Package: `gracefell@2.27.0`

## Outcome

v2.27 adds one mixed combo and one earned limit release without adding a
permanent action button, asset family, persistent progression system, or new
render surface:

- two connected light hits followed by HVY branch into **Sunder**;
- successful and varied combat fills a 100-point run-local **Resolve** meter;
- full Resolve plus a fully charged HVY releases **Gracebreak**;
- the existing HUD, touch HVY, chain lane, semantic companion, and bounded
  hybrid audio graph communicate those states;
- save schema v7, score/path identity, boss patterns, defensive timing, assets,
  music, and four-button touch geometry remain intact.

This is a compact offense layer around the established poise/Execute loop, not
a replacement for it.

## Authored combat contract

### Sunder

`ATK · ATK · HVY` becomes Sunder only after both light attacks connect.
Repeated presses do not qualify. The contact route is separate from the
presentation-only light-chain counter.

| Route | Damage | Poise | Stamina |
|---|---:|---:|---:|
| `ATK · ATK · ATK` | 50 | 50 | 36 |
| `ATK · ATK · HVY` | 50 | 70 | 44 |

The Sunder ender is 24 damage, 44 total poise, 20 stamina, a 480 ms action, and
a 600 ms decision window after the second contact. Roll has transition
priority. A missed first/second light, damage, expiry, or defensive cancel
clears the route. This makes Sunder a deliberate poise trade rather than a
strict replacement for the efficient light finisher.

### Resolve

Resolve is capped at 100 and resets with every new or abandoned fight.

| Connected event | Gain |
|---|---:|
| light 1 / light 2 / light finisher | 1 / 1 / 3 |
| Sunder | 6 |
| roll slash | 4 |
| perfect dodge | 8 |
| heavy charged to at least 70% | 6 |
| flank | 4 |
| Execute | 12 |

Flank adds to the base event. Gains are awarded only after contact and only
while Malakar remains alive. Resolve is absent from save-v7, victory scores,
best-time partitions, grade calculation, and path records.

Journey −2 and −3 receive 4 Resolve for a non-lethal wound, capped at 12 for
the attempt. Journey −1, Measured, and every Oath receive zero from wounds.

### Gracebreak

At 100 Resolve, a fully charged existing HVY becomes Gracebreak:

- 72 damage;
- 112 total poise;
- meter spent at the release frame, including on a miss;
- 20 stamina restored only after contact;
- no invulnerability, hazard clearing, teleport, or forced boss interrupt;
- partial/quick heavy preserves the full meter;
- staggered Execute takes priority and preserves the meter;
- Oath +5 remains IRONBOUND and cannot be staggered.

The whiff rule and lack of emergency defense keep the release consequential.
The Execute priority preserves the defensive mastery loop that defines the
fight.

## Interface and accessibility

- A four-segment Resolve rail sits between stamina and flasks inside the
  protected `playerHudRect()`.
- Full Resolve changes the existing touch HVY label to `BREAK` and adds one
  restrained spirit outline; there is no fifth touch target.
- Sunder availability and technique results reuse the established chain lane
  above the combat centerline.
- `Game.uiSnapshot().combat` exposes `resolvePercent`, `resolveReady`, and
  `technique`.
- The semantic game-controls panel names Resolve and the current technique
  without turning 250 ms numeric refreshes into live-announcement spam.
- The combat tips teach two-light Sunder and full-Resolve hold-HVY.

The 390×844 capture keeps player resources above the 44 px utility rail, the
boss bar above the thumb cluster, and BREAK inside the established HVY target.
The 1440×900 capture keeps the rail inside the compact upper-left resource
stack.

## Audio contract

Gameplay names five new event boundaries:

- `sunderRelease`
- `sunderHit`
- `resolveReady`
- `gracebreakRelease`
- `gracebreakHit`

Sunder and Gracebreak reuse the accepted heavy sample when ready, plus the
existing procedural fallback, spatial routing, room send, critical-voice
reservation, music ducking, phone transient, compressor, and limiter. The
Resolve-ready acknowledgement is two short tones. There is no new MP3, manifest
entry, request, loader worker, startup node, or continuous layer.

Release audio still occurs on the visible strike frame, preserving the v2.26
event-ownership rule.

## Files changed

| Area | Change |
|---|---|
| `src/game/engine.ts` | Sunder route, Resolve/Gracebreak state and priorities, HUD, touch and semantic snapshot |
| `src/game/audio.ts` | bounded named release/contact/ready audio verbs |
| `src/pages/Home.tsx` | semantic Resolve/technique state and combat tips |
| `src/game/engine.test.ts` | balance constants, gain table, Journey isolation, Sunder stretch |
| `qa/v227.cjs` | real-browser route, priority, difficulty, HUD, and audio acceptance |
| `qa/run.cjs` | mandatory v2.27 lane |
| `README.md`, `info.md`, `DESIGN.md`, `AGENTS.md`, `PROVENANCE.md` | player explanation and durable future-agent contract |

Package metadata advances from 2.26.0 to 2.27.0.

## Local acceptance

Run from a clean build on Windows with the isolated direct-loopback QA server
at `127.0.0.1:8492`.

| Gate | Result |
|---|---|
| `npm run test` | PASS — 2 files, 29 tests |
| `npm run lint` | PASS |
| `npm run build` | PASS — TypeScript and Vite production build |
| `npm run qa` | PASS — full desktop/mobile/true-touch suite |
| Core portable browser lane | PASS — zero errors |
| v2.21 mastery lane | PASS — zero errors |
| v2.24 impact-frame lane | PASS — zero errors |
| v2.27 earned-offense lane | PASS — zero errors |
| Visual upgrade/failure lane | PASS — ready, zero errors/warnings |

The v2.27 lane exercises:

1. exact three-light and two-light/Sunder damage, poise, stamina, and Resolve;
2. contact-gated route invalidation on a missed second light;
3. simultaneous roll/HVY priority;
4. full Gracebreak damage, poise, consumption, and contact stamina return;
5. whiff spending;
6. partial-heavy preservation;
7. staggered Execute precedence;
8. Journey wound cap and non-Journey isolation;
9. FORSAKEN no-stagger behavior;
10. named audio verbs and mobile/desktop HUD containment.

### Render-cost receipt

| Census | v2.26 baseline | v2.27 | Delta |
|---|---:|---:|---:|
| Mobile draw calls | 916 | 921 | +5 |
| Desktop draw calls | 898 | 903 | +5 |
| Gradients | 39 | 39 | 0 |
| `drawImage` | 2 | 2 | 0 |
| `shadowBlur` writes | 9 | 9 | 0 |

The five flat HUD operations are below the existing deterministic caps. No
weather, projectile, particle, boss, asset, or WebGL work was added.

Known build notices remain non-blocking and unchanged in kind: the query-only
Three.js proof chunk exceeds Vite's advisory 500 KiB threshold, and the local
Browserslist data is seven months old. Neither affects the production-default
Canvas path or acceptance result.

## Publication and production acceptance

Released on 2026-07-27.

| Receipt | Evidence |
|---|---|
| Runtime PR | [#91 — Add Sunder, Resolve, and Gracebreak](https://github.com/jonathanwxh-cell/gracefell/pull/91) |
| Production-receipt PR | [#92 — Record v2.27 production acceptance](https://github.com/jonathanwxh-cell/gracefell/pull/92) |
| Documentation-closure PR | [#93 — Complete the v2.27 release handoff](https://github.com/jonathanwxh-cell/gracefell/pull/93) |
| Reviewed implementation head | `d3136528a54354c2a68ec586dd10e6db1cc2ef26` |
| Runtime merge SHA | `88b71eef8ad4cafb3bac06a62f8aa3721ff33aed` |
| Reviewed-head CI | [Gracefell QA run 30244596717](https://github.com/jonathanwxh-cell/gracefell/actions/runs/30244596717), success in 3m12s |
| Runtime-main CI | [Gracefell QA run 30244816885](https://github.com/jonathanwxh-cell/gracefell/actions/runs/30244816885), success on `88b71eef...` |
| Production-receipt merge | `86f9fb3c9521e5edbf9b03c12a23c9f388b8948b` |
| Receipt-main CI | [Gracefell QA run 30245555533](https://github.com/jonathanwxh-cell/gracefell/actions/runs/30245555533), success on `86f9fb3c...` |
| Durable GitHub release | [v2.27.0](https://github.com/jonathanwxh-cell/gracefell/releases/tag/v2.27.0); the tag target is the final documentation checkpoint |
| Runtime deployment | `/home/alyosha/apps/gracefell`, clean at `88b71eef...` |
| Receipt deployment | `/home/alyosha/apps/gracefell`, clean at `86f9fb3c...`; service active from 2026-07-27 15:20:12 +08 |
| Host build | `npm run build`, pass; `assets/index-CxwiiXCN.js` |
| Service | `gracefell.service`, user-scoped, active from 2026-07-27 15:05:41 +08 |
| Loopback health | `http://127.0.0.1:8491/health` → `{"ok":true,"app":"gracefell"}` |
| Public health | `https://gracefell.alyoechosys.dev/health` → `{"ok":true,"app":"gracefell"}` |
| Public runtime | `assets/index-CxwiiXCN.js`, HTTP 200 from the uncached HTML shell |

The complete public `npm run qa` replay then targeted
`https://gracefell.alyoechosys.dev/` and passed in 262.5 seconds:

- core desktop, mobile, and true-touch gameplay: `ok:true`, zero errors;
- v2.21 mastery/migration lane: `ok:true`, zero errors;
- v2.24 impact-frame/lifecycle lane: `ok:true`, zero errors;
- v2.27 earned-offense lane: `ok:true`, zero errors;
- deterministic cost census: mobile 921 and desktop 903 draw calls, pass;
- visual and asset-failure lane: `ready`, zero errors and zero warnings.

The public v2.27 lane did not infer behavior from exports. It drove the two
three-input routes, a missed-contact invalidation, simultaneous roll/HVY,
full/partial/whiffed Gracebreak, staggered Execute precedence, Journey and
Measured wound paths, FORSAKEN poise, and both viewports against the deployed
bundle. The 390×844 public capture was inspected separately; the resources,
utility buttons, tutorial lane, boss bar, and BREAK control remained separated.

GitHub publication, clean-runner CI, exact-SHA deployment, service state,
loopback health, public health, public runtime identity, automated public
browser acceptance, and visual inspection are recorded as separate checks.
The runtime identity remains `88b71eef...`; receipt/closure merges change
documentation only. The `v2.27.0` tag resolves the final repository checkpoint
that contains this complete record.

## Rollback

Revert the v2.27 runtime merge, run the complete gate, deploy that reviewed
revert SHA, and verify loopback plus public health and browser acceptance.
There is no save migration, asset cleanup, CDN invalidation, database change,
or score repair step.

## Future-agent handoff

Treat these as load-bearing:

1. Sunder eligibility follows two connected lights, not queued presses or HUD
   chain count.
2. Roll owns simultaneous transition priority.
3. Gracebreak spends on release, restores stamina only on contact, and yields
   to staggered Execute.
4. FORSAKEN remains no-stagger.
5. Resolve resets with the fight and stays out of save-v7 and scoring.
6. Wound recovery exists only on Journey −2/−3 and caps at 12 per attempt.
7. The existing four touch buttons, protected HUD rectangles, hybrid audio
   graph, and performance census remain the expansion boundaries.
